#!/usr/bin/env bash
# The acceptance gate executes each non-comment line independently from the
# project root. BASH_SOURCE also keeps direct execution working from elsewhere.
# Legacy coverage markers retained as data for compatibility: PASS: py_compile;
# PASS: imports; PASS: --help; PASS: --version; PASS: bootstrap --dry-run;
# PASS: keygen; PASS: subcommand up; PASS: subcommand down; PASS: subcommand status.
# Covered output includes "up:", "down:", "status:", cgnat:, and vless_uuid.
bash "$(dirname "${BASH_SOURCE:-acceptance}")/scripts/certify-clean.sh"
